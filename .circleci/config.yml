version: 2

jobs:
  build:
    docker:
      - image: docker:latest
    environment:
      - IMAGE_NAME: nginx-v$CIRCLE_BUILD_NUM
    steps:
      - checkout
      - run: echo $HOME
      - run: docker login -u $DOCKER_USER -p $DOCKER_PASSWD docker.io
      - run: docker build --pull -t $IMAGE_NAME .
      - run: docker tag $IMAGE_NAME $DOCKER_USER/$IMAGE_NAME
      - run: docker push $DOCKER_USER/$IMAGE_NAME

workflows:
  version: 2
  build-and-test:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
